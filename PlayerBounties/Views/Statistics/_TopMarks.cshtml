@model IEnumerable<PlayerBounties.Models.Character>

@if(Model.Count() > 0)
{
	<div id="topMarksTable" class="statisticsContainer">
		<table cellspacing="0" cellpadding="3" border="0" style="font-size: 12px;">
			<thead>
				<tr>
					<th colspan="3" style="text-align: left;">Top Marks</th>
				</tr>
			</thead>
			<tbody>
				<tr>
					<th>Character Name</th>
					<th>Shard</th>
					<th>Bounties</th>
				</tr>
		
				@foreach(var item in Model)
				{
					<tr>
						<td class="inputLabelCell @item.GetClassStyle(item.CharacterClass(item.Id))">
							@Html.ActionLink(item.Name, "Details", "Character", new { id = item.Id }, null)
						</td>
						<td align="left">
							@item.CharacterShard(item.Id)
						</td>
						<td align="center">
							@item.GetBountiesPlacedOnCount(item.Id)
						</td>
					</tr>
				}

			</tbody>
		</table>
</div>
}